<%= form_for(alfred_module) do |f| %>
  <% if alfred_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alfred_module.errors.count, "error") %> prohibited this alfred_module from being saved:</h2>

      <ul>
      <% alfred_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :license %>
    <%= f.text_field :license %>
  </div>

  <div class="field">
    <%= f.label :homepage %>
    <%= f.text_field :homepage %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
